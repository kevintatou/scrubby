services:
  - type: web
    name: scrubby-stripe-webhook
    env: rust
    plan: free
    buildCommand: cargo build --release
    startCommand: ./target/release/stripe_webhook
    envVars:
      - key: STRIPE_WEBHOOK_SECRET
        sync: false
      - key: SCRUBBY_PRIVATE_KEY_B64
        sync: false
      - key: SCRUBBY_LICENSE_OUT_DIR
        value: /tmp/licenses
